# Archivo: monitoring/prometheus.yml

global:
  scrape_interval: 15s # Cada 15s busca métricas

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: "mi-app-flask"
    static_configs:
      - targets: ["host.docker.internal:5001"]
